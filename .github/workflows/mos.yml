name: Multi_OS

on:
  push:
    branches :
      - "master"
      - "main"

jobs:
  My_first_action:
    name: "Greetings"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - id: echo_command
      run: |
          echo "test"